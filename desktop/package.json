{
  "name": "@pokerface/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "PokerFace Windows (Tauri-обёртка вокруг веб-версии)",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
